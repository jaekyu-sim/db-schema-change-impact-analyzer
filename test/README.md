# 분석 대상 프로젝트 위치

이 폴더 아래에 분석할 Spring Boot DB Migration 프로젝트를 둡니다.

```text
test/
└─ 프로젝트명/
   ├─ pom.xml 또는 build.gradle
   └─ src/
```

그다음 저장소 루트에서 `SLLM_BASE_URL`, `SLLM_MODEL`을 설정하고 `python main.py`를 실행합니다. 결과는 `output/`에 생성됩니다.
